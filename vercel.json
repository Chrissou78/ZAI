{
  "buildCommand": "cd apps/frontend && pnpm install && ../../node_modules/.bin/vite build",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/frontend/dist",
  "env": {
    "VITE_API_URL": "/api",
    "VITE_COMPANY_ID": "zai",
    "NODE_ENV": "production"
  }
}