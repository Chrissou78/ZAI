{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm install --prod=false && cd apps/frontend && pnpm exec vite build",
  "outputDirectory": "apps/frontend/dist",
  "rewrites": [
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}