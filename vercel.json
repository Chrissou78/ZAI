{
  "buildCommand": "pnpm install --prod=false && cd apps/frontend && VITE_API_URL=$VITE_API_URL VITE_COMPANY_ID=$VITE_COMPANY_ID pnpm exec vite build",
  "installCommand": "pnpm install",
  "outputDirectory": "apps/frontend/dist",
  "env": {
    "VITE_API_URL": "@vite_api_url",
    "VITE_COMPANY_ID": "@vite_company_id"
  }
}